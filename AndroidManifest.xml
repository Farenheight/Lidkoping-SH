<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lidkopingsh"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="18" />

    <instrumentation
        android:name="android.test.InstrumentationTestRunner"
        android:label="Test"
        android:targetPackage="com.example.lidkopingsh" >
    </instrumentation>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.lidkopingsh.StoneListActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustNothing" >
        </activity>
        <activity
            android:name="com.example.lidkopingsh.tabs.TabActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustNothing" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.lidkopingsh.StoneDetailActivity"
            android:label="@string/title_stone_detail"
            android:parentActivityName=".StoneListActivity"
            android:windowSoftInputMode="adjustNothing" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".StoneListActivity" />
        </activity>

        <uses-library android:name="android.test.runner" />
    </application>

</manifest>